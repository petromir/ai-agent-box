# VCS metadata never belongs in a build context
.git
.gitignore

# IDE state
.idea
.vscode

# Docs and license are not used by the build
*.md
LICENSE

# Container artifacts (opencode/opencode-entrypoint.sh and omp/omp-entrypoint.sh ARE
# copied by the Dockerfiles — keep them)
opencode/opencode.Dockerfile
omp/omp.Dockerfile
java/java.25.Dockerfile
docker-compose*
.dockerignore
