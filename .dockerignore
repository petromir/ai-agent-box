# VCS metadata never belongs in a build context
.git
.gitignore

# IDE state
.idea
.vscode

# Docs and license are not used by the build
*.md
LICENSE

# Container artifacts (entrypoint.sh and omp-entrypoint.sh ARE copied by the
# Dockerfiles — keep them)
opencode.Dockerfile
docker-compose*
.dockerignore
